# 🔧 ИСПРАВЛЕНИЕ ОШИБОК - oleana.pythonanywhere.com

## ШАГ 1: Проверьте логи ошибок

1. Откройте вкладку **"Web"** на PythonAnywhere
2. Прокрутите вниз до раздела **"Log files"**
3. Нажмите на **"Error log"** (красная ссылка)
4. Посмотрите последние 20-30 строк - там будет описание ошибки

**Частые ошибки и решения ниже ⬇️**

---

## ОШИБКА: "No module named 'knitting_project'"

### РЕШЕНИЕ:

1. Откройте вкладку **"Web"**
2. Найдите **"WSGI configuration file"** и кликните
3. Убедитесь что код выглядит ТАК (замените oleana на ваш username):

```python
import os
import sys

# ВАЖНО: oleana - это ваш username
path = '/home/oleana'
if path not in sys.path:
    sys.path.insert(0, path)

os.environ['DJANGO_SETTINGS_MODULE'] = 'knitting_project.settings'

from django.core.wsgi import get_wsgi_application
application = get_wsgi_application()
```

4. Нажмите **Save** (зеленая кнопка)
5. Вернитесь на вкладку Web
6. Нажмите **Reload oleana.pythonanywhere.com**

---

## ОШИБКА: "No module named 'django'"

### РЕШЕНИЕ:

1. Вкладка **"Consoles"** → **"Bash"**
2. Выполните:

```bash
python3 -m pip install django --user
```

3. Вернитесь на вкладку **Web**
4. Нажмите **Reload**

---

## ОШИБКА: Database error / "no such table"

### РЕШЕНИЕ:

1. Консоль **Bash**:

```bash
cd /home/oleana
python3 manage.py migrate
python3 add_test_data.py
```

2. Reload на вкладке Web

---

## ОШИБКА: "Something went wrong :("

### РЕШЕНИЕ:

Проверьте все настройки на вкладке **"Web"**:

### 1. Code section:
- **Source code:** `/home/oleana`
- **Working directory:** `/home/oleana`

### 2. Virtualenv:
Можно оставить ПУСТЫМ

### 3. Static files:
- **URL:** `/static/`
- **Directory:** `/home/oleana/staticfiles`

### 4. WSGI Configuration File:
Должен содержать правильный код (см. выше)

---

## ЕСЛИ ВСЕ ЕЩЕ НЕ РАБОТАЕТ

### Полная переустановка (займет 5 минут):

```bash
# 1. Откройте консоль Bash
cd /home/oleana

# 2. Проверьте файлы
ls -la

# Вы должны увидеть:
# - manage.py
# - knitting/ (папка)
# - knitting_project/ (папка)
# - requirements.txt

# 3. Если файлов нет - распакуйте заново:
unzip knitting_site_upload.zip

# 4. Установите Django
python3 -m pip install django --user

# 5. Настройте БД
python3 manage.py migrate
python3 add_test_data.py

# 6. Соберите static
python3 manage.py collectstatic --noinput

# 7. Проверка
python3 manage.py check
```

После этого:
- Вкладка Web
- Проверьте все настройки (см. выше)
- Reload

---

## ПРОВЕРЬТЕ ЭТИ ПУНКТЫ:

✅ **Files:** Все файлы распакованы в `/home/oleana/`
✅ **Django:** Установлен (`python3 -m pip install django --user`)
✅ **Migrations:** Выполнены (`python3 manage.py migrate`)
✅ **Static:** Собраны (`python3 manage.py collectstatic`)
✅ **Web app:** Создан (Manual configuration, Python 3.10)
✅ **Source code:** `/home/oleana`
✅ **Working directory:** `/home/oleana`
✅ **WSGI:** path = '/home/oleana' (не ваш_username, а oleana!)
✅ **Static files:** URL = `/static/`, Directory = `/home/oleana/staticfiles`

---

## КОМАНДА ДЛЯ ДИАГНОСТИКИ:

Выполните в консоли Bash:

```bash
cd /home/oleana
echo "=== Checking files ==="
ls -la
echo ""
echo "=== Checking Django ==="
python3 -c "import django; print('Django version:', django.get_version())"
echo ""
echo "=== Checking project ==="
python3 manage.py check
echo ""
echo "=== Checking database ==="
ls -la db.sqlite3
```

Скопируйте вывод и посмотрите, где ошибка!

---

## НУЖНА ПОМОЩЬ?

1. Посмотрите **Error log** на вкладке Web
2. Выполните диагностику (команда выше)
3. Проверьте чеклист
4. Попробуйте полную переустановку

**После каждого изменения - нажимайте Reload!** 🔄
