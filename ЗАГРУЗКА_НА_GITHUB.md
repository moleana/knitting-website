# 🚀 КАК ЗАГРУЗИТЬ ПРОЕКТ НА GITHUB

## 📋 Что вам понадобится

1. Аккаунт на GitHub (бесплатный)
2. Git установлен на компьютере (уже есть!)
3. 10-15 минут времени

---

## 🎯 Шаг 1: Создайте репозиторий на GitHub

### 1.1 Регистрация/Вход на GitHub

1. Откройте https://github.com/
2. Если нет аккаунта:
   - Нажмите **"Sign up"**
   - Введите email, пароль, username
   - Подтвердите email
3. Если есть аккаунт - нажмите **"Sign in"**

### 1.2 Создание нового репозитория

1. После входа нажмите **"+"** (вверху справа)
2. Выберите **"New repository"**
3. Заполните форму:
   - **Repository name:** `knitting-website` (или любое название)
   - **Description:** `Сайт мастерской вязания на Django`
   - **Public** или **Private** - выберите на ваш выбор
     - Public - все смогут видеть код
     - Private - только вы
   - **НЕ СТАВЬТЕ ГАЛОЧКИ** на:
     - ❌ Add a README file
     - ❌ Add .gitignore
     - ❌ Choose a license
4. Нажмите **"Create repository"**

### 1.3 Скопируйте URL репозитория

После создания вы увидите страницу с инструкциями.
Найдите и **скопируйте URL** вида:
```
https://github.com/ваш_username/knitting-website.git
```

**СОХРАНИТЕ ЭТОТ URL!** Он понадобится дальше.

---

## 💻 Шаг 2: Инициализируйте Git в проекте

Откройте терминал (PowerShell) в папке проекта `knitting_site` и выполните команды:

### 2.1 Инициализация Git

```bash
git init
```

Должно появиться: `Initialized empty Git repository in...`

### 2.2 Добавьте все файлы

```bash
git add .
```

Эта команда добавляет все файлы проекта (кроме тех, что в .gitignore)

### 2.3 Сделайте первый коммит

```bash
git commit -m "Первая версия сайта вязания"
```

Должно появиться сообщение о созданных файлах.

### 2.4 Переименуйте ветку в main

```bash
git branch -M main
```

---

## 🔗 Шаг 3: Свяжите с GitHub и загрузите

### 3.1 Добавьте remote репозиторий

**ЗАМЕНИТЕ URL** на тот, который вы скопировали на шаге 1.3!

```bash
git remote add origin https://github.com/ваш_username/knitting-website.git
```

Пример:
```bash
git remote add origin https://github.com/alina123/knitting-website.git
```

### 3.2 Загрузите код на GitHub

```bash
git push -u origin main
```

**Что может произойти:**
- GitHub попросит ввести логин и пароль
- **ВАЖНО:** Вместо пароля нужен **Personal Access Token**

---

## 🔑 Шаг 4: Создание Personal Access Token (если нужно)

Если при `git push` GitHub просит авторизацию:

### 4.1 Создайте токен

1. На GitHub: кликните на свой аватар (вверху справа)
2. **Settings** → **Developer settings** (внизу слева)
3. **Personal access tokens** → **Tokens (classic)**
4. **Generate new token** → **Generate new token (classic)**
5. Заполните:
   - **Note:** `knitting-site-upload`
   - **Expiration:** `90 days` (или больше)
   - **Select scopes:** поставьте галочку на **repo** (все подпункты)
6. Прокрутите вниз и нажмите **"Generate token"**
7. **СКОПИРУЙТЕ ТОКЕН!** Он больше не появится!
   - Выглядит так: `ghp_xxxxxxxxxxxxxxxxxxxx`

### 4.2 Используйте токен при push

При запросе пароля вставьте **токен** (не ваш пароль GitHub!)

```bash
git push -u origin main
```

- **Username:** ваш GitHub username
- **Password:** вставьте токен (ghp_...)

---

## ✅ Шаг 5: Проверка

1. Откройте ваш репозиторий на GitHub:
   ```
   https://github.com/ваш_username/knitting-website
   ```

2. Вы должны увидеть все файлы проекта!

3. Проверьте, что есть:
   - ✅ Папка `knitting/`
   - ✅ Папка `knitting_project/`
   - ✅ Файл `manage.py`
   - ✅ Файл `requirements.txt`
   - ✅ Все .md инструкции

4. Проверьте, что **НЕТ** (они в .gitignore):
   - ❌ `db.sqlite3` (базы данных)
   - ❌ Папки `venv/`
   - ❌ Файлов `*.pyc`

---

## 🔄 Как обновлять код на GitHub в будущем

После изменений в проекте:

### Вариант 1: Все изменения сразу

```bash
# Добавить все изменённые файлы
git add .

# Сделать коммит с описанием изменений
git commit -m "Обновил ссылки на видео уроков"

# Загрузить на GitHub
git push
```

### Вариант 2: Конкретные файлы

```bash
# Добавить конкретный файл
git add add_test_data.py

# Коммит
git commit -m "Изменил данные в add_test_data.py"

# Загрузить
git push
```

---

## 📝 Примеры коммитов

Хорошие сообщения коммитов:

```bash
git commit -m "Добавил новый урок по вязанию кос"
git commit -m "Исправил ссылки в навигации"
git commit -m "Обновил цены на пряжу"
git commit -m "Добавил админ-панель инструкцию"
```

---

## 💡 Полезные команды Git

### Проверить статус (что изменилось)
```bash
git status
```

### Посмотреть историю коммитов
```bash
git log --oneline
```

### Отменить последние изменения (до коммита)
```bash
git checkout .
```

### Посмотреть remote репозиторий
```bash
git remote -v
```

---

## 🎨 Бонус: Сделайте красивый README на GitHub

Создайте файл `README.md` в корне проекта (или отредактируйте существующий):

```markdown
# 🧶 Сайт Мастерской Вязания

Веб-сайт для любителей вязания с уроками, схемами и информацией о пряже.

## 🚀 Технологии

- Python 3.10
- Django 5.2
- SQLite

## 📦 Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ваш_username/knitting-website.git
cd knitting-website
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
```

3. Запустите миграции:
```bash
python manage.py migrate
python add_test_data.py
```

4. Запустите сервер:
```bash
python manage.py runserver
```

5. Откройте в браузере: http://127.0.0.1:8000/

## 🌐 Демо

Сайт доступен по адресу: https://ваш_username.pythonanywhere.com

## 📝 Возможности

- 📐 Схемы вязания с разными уровнями сложности
- 🎥 Видео-уроки для начинающих и продвинутых
- 🧵 Каталог пряжи с ценами
- 🔐 Админ-панель для управления контентом

## 🤝 Автор

Ваше имя - Мастерская Вязания
```

Загрузите обновленный README:
```bash
git add README.md
git commit -m "Обновил README"
git push
```

---

## ❌ Возможные проблемы и решения

### Ошибка: "remote origin already exists"
```bash
git remote remove origin
git remote add origin https://github.com/ваш_username/knitting-website.git
```

### Ошибка: "failed to push"
```bash
# Сначала получите изменения
git pull origin main --allow-unrelated-histories
# Потом загрузите
git push
```

### Забыли что-то добавить в .gitignore
```bash
# Удалите из git (но не с диска)
git rm --cached db.sqlite3
git commit -m "Удалил базу данных из git"
git push
```

### Нужно сохранить токен, чтобы не вводить каждый раз
```bash
git config --global credential.helper store
```
При следующем `git push` введите токен, и он сохранится.

---

## 🎯 Краткая шпаргалка

```bash
# ПЕРВАЯ ЗАГРУЗКА
git init
git add .
git commit -m "Первый коммит"
git branch -M main
git remote add origin https://github.com/username/repo.git
git push -u origin main

# ОБНОВЛЕНИЯ
git add .
git commit -m "Описание изменений"
git push
```

---

## 🌟 Готово!

Теперь ваш проект на GitHub! 

**Ссылка на репозиторий:**
```
https://github.com/ваш_username/knitting-website
```

Поделитесь ссылкой с друзьями! 🎉

**Удачи! 🚀**
