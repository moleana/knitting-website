# 🔄 ОБНОВЛЕНИЕ ДАННЫХ НА PYTHONANYWHERE

## Проблема
Локально данные обновились, но на сервере PythonAnywhere остались старые данные.

## ✅ Решение - 3 простых шага

---

## Шаг 1: Загрузите обновленный файл

### Вариант А: Через веб-интерфейс (ПРОЩЕ)

1. Войдите на https://www.pythonanywhere.com/
2. Перейдите на вкладку **"Files"**
3. Найдите файл **add_test_data.py** в списке файлов
4. Кликните на него (откроется редактор)
5. **Скопируйте весь код** из вашего локального файла `add_test_data.py`
6. **Вставьте** в редактор на PythonAnywhere (заменив весь старый код)
7. Нажмите **"Save"** (зеленая кнопка вверху)

### Вариант Б: Загрузить файл

1. Перейдите на вкладку **"Files"**
2. Нажмите **"Upload a file"**
3. Выберите файл **add_test_data.py** с вашего компьютера
4. Подтвердите замену файла

---

## Шаг 2: Обновите базу данных

1. Откройте вкладку **"Consoles"**
2. Нажмите **"Bash"** (или откройте существующую консоль)
3. Выполните команды:

```bash
# Перейдите в корневую папку (замените ваш_username!)
cd /home/ваш_username

# Очистите базу данных
python3 manage.py flush --noinput

# Запустите скрипт с новыми данными
python3 add_test_data.py
```

Должно появиться:
```
✅ Тестовые данные успешно добавлены!
Добавлено 5 схем вязания
Добавлено 4 типов пряжи
Добавлено 5 уроков
```

---

## Шаг 3: Перезагрузите сайт

1. Перейдите на вкладку **"Web"**
2. Нажмите большую зеленую кнопку **"Reload ваш_username.pythonanywhere.com"**
3. Подождите 5-10 секунд
4. Откройте ваш сайт и проверьте!

---

## 🎯 Проверка

После обновления проверьте:

1. Откройте **ваш_username.pythonanywhere.com/tutorials/**
2. Найдите урок **"Набор петель классическим способом"**
3. Нажмите **"Смотреть видео"**
4. Должно открыться видео с новой ссылкой: `https://www.youtube.com/watch?v=3SVRP5Klo18`

---

## ⚡ Быстрый способ (только команды)

Если файл уже загружен, просто выполните в консоли Bash:

```bash
cd /home/ваш_username
python3 manage.py flush --noinput
python3 add_test_data.py
```

Потом Reload на вкладке Web.

---

## ❌ Возможные проблемы

### "No such file or directory"
- Проверьте, что вы в правильной папке: `pwd`
- Должно показать: `/home/ваш_username`

### "Permission denied"
- Используйте `python3` вместо `python`

### База данных не обновляется
```bash
# Попробуйте удалить базу и пересоздать
rm db.sqlite3
python3 manage.py migrate
python3 add_test_data.py
```

### Сайт показывает ошибку
1. На вкладке Web кликните **"Error log"**
2. Посмотрите последние строки
3. Если ошибка "Database", повторите миграции:
```bash
python3 manage.py migrate
python3 add_test_data.py
```

---

## 📝 Важно знать

### Когда нужно обновлять данные?

**ДА - нужно обновить:**
- ✅ Изменили `add_test_data.py` (названия, описания, ссылки)
- ✅ Добавили новые уроки, схемы или пряжу
- ✅ Удалили старые данные

**НЕТ - просто Reload:**
- ❌ Изменили HTML шаблоны (base.html, home.html и т.д.)
- ❌ Изменили CSS стили
- ❌ Изменили views.py или urls.py

### Как часто делать backup?

Если у вас есть важные данные:
```bash
# Сделайте копию базы данных
cp db.sqlite3 db.sqlite3.backup_20_12_2024
```

---

## 🔄 В будущем

Чтобы избежать необходимости каждый раз очищать базу:

### Добавление новых данных (не удаляя старые):

Создайте новый файл `add_more_data.py`:
```python
import os
import django

os.environ.setdefault('DJANGO_SETTINGS_MODULE', 'knitting_project.settings')
django.setup()

from knitting.models import Tutorial

# Добавить новый урок (не удаляя старые)
new_tutorial = Tutorial(
    title="Новый урок",
    content="Описание",
    video_url="https://youtube.com/...",
    duration=20
)
new_tutorial.save()
```

### Обновление конкретного урока через админку:

1. Войдите в админку: `ваш_username.pythonanywhere.com/admin`
2. Найдите нужный урок
3. Измените ссылку
4. Сохраните
5. Reload сайта

---

## ✨ Готово!

Теперь ваши изменения должны отобразиться на сайте!

**Удачи! 🚀**
