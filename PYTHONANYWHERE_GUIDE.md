# 🌟 Пошаговая инструкция: PythonAnywhere

## Шаг 1: Регистрация (2 минуты)

1. Откройте https://www.pythonanywhere.com/
2. Нажмите **"Pricing & signup"**
3. Выберите **"Create a Beginner account"** (бесплатно!)
4. Заполните форму:
   - Username (запомните его!)
   - Email
   - Password
5. Подтвердите email

## Шаг 2: Загрузка файлов (5 минут)

### Вариант А: Через интерфейс (проще)

1. Войдите в свой аккаунт PythonAnywhere
2. Перейдите на вкладку **"Files"**
3. Создайте папку **knitting_site**
4. Загрузите туда все файлы проекта (кроме папки `venv`):
   - Все файлы .py
   - Папку `knitting` (целиком)
   - Папку `knitting_project` (целиком)
   - requirements.txt
   - add_test_data.py

### Вариант Б: Через Git (если есть репозиторий)

1. Перейдите на вкладку **"Consoles"**
2. Нажмите **"Bash"**
3. Выполните:
```bash
git clone https://github.com/ваш-username/knitting_site.git
cd knitting_site
```

## Шаг 3: Настройка окружения (3 минуты)

1. Откройте **"Consoles"** → **"Bash"**
2. Выполните команды:

```bash
# Создание виртуального окружения
mkvirtualenv --python=/usr/bin/python3.10 knitting_env

# Переход в папку проекта
cd knitting_site

# Установка зависимостей
pip install django

# Миграции базы данных
python manage.py migrate

# Добавление тестовых данных
python add_test_data.py

# Сбор статических файлов
python manage.py collectstatic --noinput
```

## Шаг 4: Создание Web App (5 минут)

1. Перейдите на вкладку **"Web"**
2. Нажмите **"Add a new web app"**
3. Нажмите **"Next"** (согласитесь с доменом)
4. Выберите **"Manual configuration"**
5. Выберите **"Python 3.10"**
6. Нажмите **"Next"**

## Шаг 5: Настройка путей

### Code (Код):
- **Source code:** `/home/ваш_username/knitting_site`
- **Working directory:** `/home/ваш_username/knitting_site`

### Virtualenv:
- Путь: `/home/ваш_username/.virtualenvs/knitting_env`
- Или создайте новый, нажав "Enter path to a virtualenv"

## Шаг 6: Настройка WSGI файла (ВАЖНО!)

1. В разделе **"Code"** найдите **"WSGI configuration file"**
2. Нажмите на ссылку (например, `/var/www/username_pythonanywhere_com_wsgi.py`)
3. **УДАЛИТЕ весь код** в файле
4. **Вставьте этот код** (замените `ваш_username` на ваш логин):

```python
import os
import sys

# Добавляем путь к вашему проекту
path = '/home/ваш_username/knitting_site'
if path not in sys.path:
    sys.path.insert(0, path)

# Указываем настройки Django
os.environ['DJANGO_SETTINGS_MODULE'] = 'knitting_project.settings'

# Импортируем приложение Django
from django.core.wsgi import get_wsgi_application
application = get_wsgi_application()
```

5. Нажмите **"Save"** (зеленая кнопка вверху)

## Шаг 7: Настройка статических файлов

В разделе **"Static files"** добавьте:
- **URL:** `/static/`
- **Directory:** `/home/ваш_username/knitting_site/staticfiles`

## Шаг 8: Запуск! 🚀

1. Прокрутите страницу наверх
2. Найдите большую зеленую кнопку **"Reload ваш_username.pythonanywhere.com"**
3. Нажмите её!
4. Подождите 5-10 секунд
5. Нажмите на ссылку **`ваш_username.pythonanywhere.com`**

**Готово! Ваш сайт в интернете! 🎉**

## 🔧 Если что-то пошло не так

### Ошибка 500 или сайт не открывается?

1. Перейдите на вкладку **"Web"**
2. Посмотрите **"Error log"** и **"Server log"**
3. Проверьте основные причины:

**Проблема:** ImportError
- Решение: Убедитесь, что в WSGI файле правильный username
- Проверьте пути в разделе "Code"

**Проблема:** Static files не загружаются
- Решение: Выполните `python manage.py collectstatic` в консоли
- Проверьте путь в разделе "Static files"

**Проблема:** Database error
- Решение: Убедитесь, что выполнили `python manage.py migrate`

### Нужна помощь?
1. Посмотрите логи (Error log, Server log)
2. Проверьте все пути - они должны начинаться с `/home/ваш_username/`
3. Перезагрузите web app (зеленая кнопка "Reload")

## 📝 Полезные советы

### Как обновить сайт:
1. Измените файлы через вкладку "Files"
2. Нажмите "Reload" на вкладке "Web"

### Как добавить данные в админку:
1. Создайте суперпользователя в консоли:
```bash
cd knitting_site
python manage.py createsuperuser
```
2. Перейдите на `ваш_username.pythonanywhere.com/admin/`

### Как изменить домен:
- Бесплатный план: только `username.pythonanywhere.com`
- Платный план: можно использовать свой домен

## ✅ Готово!

Теперь у вас есть постоянная ссылка на сайт:
**`https://ваш_username.pythonanywhere.com`**

Поделитесь ей с кем угодно! Сайт работает 24/7! 🌐
