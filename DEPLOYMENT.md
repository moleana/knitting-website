# 🌐 Инструкция по развертыванию сайта

Здесь описаны несколько бесплатных способов развернуть ваш Django сайт, чтобы он был доступен по ссылке.

## 🎯 Вариант 1: PythonAnywhere (Рекомендуется для начинающих)

**PythonAnywhere** - бесплатный хостинг для Python приложений с простой настройкой.

### Шаги:

1. **Регистрация**
   - Перейдите на https://www.pythonanywhere.com/
   - Нажмите "Start running Python online in less than a minute!"
   - Создайте бесплатный аккаунт (Beginner account)

2. **Загрузка файлов**
   - Откройте вкладку "Files"
   - Загрузите все файлы проекта (кроме venv и __pycache__)
   - Или используйте Git (рекомендуется):
     ```bash
     # В консоли PythonAnywhere:
     git clone <ссылка-на-ваш-репозиторий>
     cd knitting_site
     ```

3. **Создание виртуального окружения**
   - Откройте вкладку "Consoles" → "Bash"
   - Выполните:
     ```bash
     mkvirtualenv --python=/usr/bin/python3.10 knitting_env
     cd knitting_site
     pip install -r requirements.txt
     ```

4. **Миграции и тестовые данные**
   ```bash
   python manage.py migrate
   python add_test_data.py
   python manage.py collectstatic --noinput
   ```

5. **Настройка Web App**
   - Откройте вкладку "Web"
   - Нажмите "Add a new web app"
   - Выберите "Manual configuration" → Python 3.10
   - В разделе "Code":
     - Source code: `/home/ваш_username/knitting_site`
     - Working directory: `/home/ваш_username/knitting_site`
   - В разделе "Virtualenv":
     - Путь: `/home/ваш_username/.virtualenvs/knitting_env`

6. **Настройка WSGI файла**
   - Нажмите на ссылку WSGI configuration file
   - Удалите весь код и замените на:
   ```python
   import os
   import sys
   
   path = '/home/ваш_username/knitting_site'
   if path not in sys.path:
       sys.path.append(path)
   
   os.environ['DJANGO_SETTINGS_MODULE'] = 'knitting_project.settings'
   
   from django.core.wsgi import get_wsgi_application
   application = get_wsgi_application()
   ```
   - Сохраните файл

7. **Настройка статических файлов**
   - В разделе "Static files":
     - URL: `/static/`
     - Directory: `/home/ваш_username/knitting_site/staticfiles`

8. **Запуск**
   - Нажмите зеленую кнопку "Reload ваш_username.pythonanywhere.com"
   - Перейдите по ссылке `https://ваш_username.pythonanywhere.com`

---

## 🎯 Вариант 2: Render (Современная платформа)

**Render** - современный хостинг с автоматическим развертыванием из Git.

### Шаги:

1. **Подготовка проекта**
   - Создайте файл `build.sh` в корне проекта:
     ```bash
     #!/usr/bin/env bash
     pip install -r requirements.txt
     python manage.py collectstatic --noinput
     python manage.py migrate
     python add_test_data.py
     ```
   - Сделайте его исполняемым (Linux/Mac): `chmod +x build.sh`

2. **Создание Git репозитория**
   ```bash
   git init
   git add .
   git commit -m "Initial commit"
   ```
   - Загрузите на GitHub или GitLab

3. **Регистрация на Render**
   - Перейдите на https://render.com/
   - Создайте бесплатный аккаунт

4. **Создание Web Service**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш Git репозиторий
   - Настройки:
     - Name: `knitting-site`
     - Environment: `Python 3`
     - Build Command: `./build.sh`
     - Start Command: `gunicorn knitting_project.wsgi:application`
   - Добавьте в requirements.txt: `gunicorn`

5. **Environment Variables**
   - Добавьте переменные окружения:
     - `PYTHON_VERSION`: `3.10.0`

6. **Деплой**
   - Нажмите "Create Web Service"
   - Render автоматически развернет ваш сайт
   - Сайт будет доступен по ссылке типа: `https://knitting-site.onrender.com`

---

## 🎯 Вариант 3: Railway (Простой и быстрый)

**Railway** - очень простая платформа с автоматическим развертыванием.

### Шаги:

1. **Подготовка**
   - Добавьте `gunicorn` в requirements.txt
   - Создайте файл `Procfile` в корне:
     ```
     web: gunicorn knitting_project.wsgi --log-file -
     ```

2. **Создание Git репозитория** (если еще не создан)
   ```bash
   git init
   git add .
   git commit -m "Ready for deployment"
   ```

3. **Регистрация на Railway**
   - Перейдите на https://railway.app/
   - Войдите через GitHub

4. **Деплой**
   - Нажмите "New Project" → "Deploy from GitHub repo"
   - Выберите ваш репозиторий
   - Railway автоматически определит Django и развернет сайт
   - После развертывания нажмите "Generate Domain"

---

## 🎯 Вариант 4: Быстрое тестирование с ngrok

Для временного доступа к локальному серверу:

1. **Скачайте ngrok**
   - https://ngrok.com/download

2. **Запустите сервер Django**
   ```bash
   python manage.py runserver
   ```

3. **В другом терминале запустите ngrok**
   ```bash
   ngrok http 8000
   ```

4. **Получите ссылку**
   - ngrok выдаст временную ссылку типа: `https://abc123.ngrok.io`
   - Поделитесь ей с друзьями!
   - ⚠️ Ссылка работает только пока запущен ngrok

---

## 📝 Важные замечания

### Для production:
1. В `settings.py` измените:
   ```python
   DEBUG = False
   ALLOWED_HOSTS = ['ваш-домен.com', 'www.ваш-домен.com']
   ```

2. Создайте новый SECRET_KEY:
   ```python
   import secrets
   secrets.token_urlsafe(50)
   ```

3. Используйте переменные окружения для секретных данных

### База данных:
- SQLite работает для небольших проектов
- Для больших нагрузок используйте PostgreSQL
- PythonAnywhere поддерживает MySQL бесплатно

---

## 🆘 Помощь

Если возникли проблемы:
1. Проверьте логи на платформе хостинга
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки ALLOWED_HOSTS
4. Для PythonAnywhere: проверьте WSGI файл

**Удачи с развертыванием! 🚀**
